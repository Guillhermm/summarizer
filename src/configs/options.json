{
  "apiKey": {
    "label": "OpenAI API Key",
    "placeholder": "API Key",
    "text": "Optional — used as a fallback when Chrome's built-in AI is not available on your device.",
    "textHelper": "Once the key is filled in, press tab to validate it",
    "type": "password"
  }
}
